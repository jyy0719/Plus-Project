<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration
  PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-config.dtd">
  
<configuration>

	<!-- Properties 파일 설정 -->
	<!--  <properties resource="mysql-db.properties"/> -->
	
	<!-- Alias 필요시 설정 -->
<typeAliases>
		<typeAlias alias="club" type="com.project.plus.domain.ClubVO"/>
		<typeAlias alias="user" type="com.project.plus.domain.MemberVO"/>
		<typeAlias alias="review" type="com.project.plus.domain.ReviewVO"/>
		<typeAlias alias="announce" type="com.project.plus.domain.AnnounceVO"/>
		<typeAlias alias="apply" type="com.project.plus.domain.ApplyVO"/>
		<typeAlias alias="faq" type="com.project.plus.domain.FaqVO"/>
		<typeAlias alias="inquiry" type="com.project.plus.domain.InquiryVO"/>
	
</typeAliases>


	<!--  아래 설정들은 root-context에서 이미 해주므로 생략해야해요  -->
	<!-- DataSource 설정 -->
<!-- 	<environments default="development">
		<environment id="development">
			<transactionManager type="JDBC"/>
			<dataSource type="POOLED">
				<property name="driver" value="${jdbc.driverClassName}"/>
				<property name="url" value="${jdbc.url}"/>
				<property name="username" value="${jdbc.username}"/>
				<property name="password" value="${jdbc.password}"/>
			</dataSource>
		</environment>
	</environments> -->
			
	<!-- Sql Mapper 설정-->
	<!-- <mappers>
		<mapper resource="mappings/faq-mapper.xml"/>
	</mappers> -->
</configuration>
